# AI Learning Projects

I'm a software engineer working my way toward applied AI engineering. This
repo is where that happens: one small project at a time, each one rebuilding
a concept I've been using through libraries until I actually understand it.

The rules I'm holding myself to:

- Every project is built from scratch with plain code — no copying model
  glue from tutorials, no letting an LLM write the core logic for me.
- Every project has real tests and some kind of evaluation, because "it
  looks right" is how I've fooled myself before.
- Every README is honest about what went wrong, not just what worked.

Projects build on each other where it makes sense — 03 exists because of
the two paraphrase queries 02 couldn't answer. [progress.md](progress.md)
tracks what's done, which mechanisms already exist in the repo (so nothing
gets rebuilt twice by accident), and the open questions each project left
behind — those open questions are where the next projects come from.

## Projects

| # | Project | Concept | One-line takeaway |
|---|---------|---------|-------------------|
| 26 | [reranking](26-reranking/) | Two-stage retrieval: a BM25 or LSA shortlist reranked by the pooled bi-encoder, a late-interaction MaxSim scorer, and a label-reading oracle that prices the shortlist ceiling, with latent-dot cost accounting and a depth sweep (Python) | Reranking BM25's top 20 with the pooled bi-encoder reproduces the full dense scan's reciprocal rank on every one of the 40 queries at a fifth of the latent dots and ties RRF fusion, but the +0.011 over plain BM25 has p(diff <= 0) = 0.1287, so on this set only the cost saving is established, not the gain; MaxSim over per-term vectors from the very same LSA fit is nominally the best real scorer (0.867 vs BM25's 0.851) but at ~95x the pooled scorer's cost for a gap of +0.016 [-0.042, +0.075] that the bootstrap cannot separate from zero, so untrained term-level granularity buys price, not rank, while exact-match cosine ties plus a stable sort leave every keyword query untouched at 0.950 under every scorer; and the oracle shows the headroom lives in the scorer, not the shortlist: gold is already in the top 20 for 95% of queries, and +0.089 [+0.025, +0.160] separates a perfect pair scorer from the pooled one, the only gap in the table that clears zero. |
| 25 | [query-rewriting](25-query-rewriting/) | Query rewriting / HyDE measured against the raw query — authored hypothetical answers with a seeded hallucination knob, PRF expansion, and an append-vs-replace anchor comparison over BM25 (Python) | A knowledgeable rewrite closes the vocabulary-mismatch gap (paraphrase MRR@10 0.736 → 0.967, and the "GIL" query goes from matching nothing to a perfect hit), but the value is entirely conditional on knowledge: fluent generic filler drops MRR to 0.560 — worse than not rewriting at all — a 10% hallucination rate is survivable in both modes (replace 0.897, append 0.919) but replace crosses under the raw query between 15% and 17.5% while keeping the query text as an anchor carries append to 22.5–25%, and PRF barely moves (+0.008, CI touching zero) because on 32 of 40 queries the first search's top doc was already gold, and terms from a doc you already rank first cannot move it up. |
| 24 | [extraction-metrics](24-extraction-metrics/) | Field-level precision/recall for structured extraction — every JSON leaf scored correct/wrong/missing/spurious, with a value-normalization ladder and order-insensitive array alignment, audited against scripted extractors whose damage is authored and known (TypeScript) | Record-level exact match scores a differently-formatted perfect extraction, a single dead field, and 27% corruption identically at 0.000, while field-level scoring separates them (semantic F1 1.000 / 0.946 / 0.728): the normalization ladder rescues pure format drift stepwise (0.219 → 0.397 → 0.946 → 1.000) and never moves the corruptor — the safety check that no forgiveness layer forgives real errors — greedy alignment recovers a shuffled line-item list from 0.647 to 1.000 with zero delta on every other failure class, precision names the hallucinator (0.665) and recall the dropper (0.759), a micro F1 of 0.946 hides a 0.000 totals.tax row that only the per-field table names, and macro-over-gold-paths turns out blind to hallucinated structure, ranking the field-inventor above its own micro score because invented paths have no gold row to average. |
| 23 | [multi-hop-retrieval](23-multi-hop-retrieval/) | Iterative two-hop retrieval over BM25: extract bridge terms from the top doc with tf*idf, requery, merge — with an oracle-bridge ablation and drift accounting on an authored corpus where answers hide behind a bridge entity (Python) | The second search buys what the first one cannot see: answer recall@5 jumps 0.667 to 0.958 (append) and 1.000 (bridge-terms-only) at exactly 2.00 searches per query, with the paired bootstrap putting the mrr gap at +0.080 [+0.043, +0.119] — but that is the only gap here that clears zero, bridge-terms-only over keeping the question is +0.010 [-0.011, +0.032] and rests on 4 queries with one of them pointing the other way, the hop1-first merge pins recall@1 at 0.083 for every system including the oracle, the one authored drift trap that fired was a vocabulary-island echo the interleave deduped away for free, and 8 blind single-hop controls paid 2x for zero gain — the router question in one number. |
| 22 | [rag-vertical-slice](22-rag-vertical-slice/) | Vertical RAG slice: one live endpoint that retrieves over a doc set, answers with a scripted extractive reader, streams the answer as SSE through a bounded queue, logs tokens and cost per request, and grades itself with a golden-set eval hook over real HTTP (TypeScript) | Composition is where the numbers live: retrieval hit@k climbs 0.650 to 0.950 across k=1..5 while answer accuracy only crawls 0.350 to 0.475, because the lexical reader extracts the right sentence from a retrieved-gold context just half the time and every extra doc is another set of distractor sentences (keyword 0.700 vs paraphrase 0.200 at k=3, the same lexical machinery failing on both sides of the pipeline), k=1 to k=3 buys 0.100 accuracy for 2.85x the input tokens, a streaming client renders its first token at a mean 22.6% of the response bytes, and against a slow client a bounded queue caps buffering where an unbounded one piles up the whole response (17895 bytes on the worst case) — while the refusal floor keeps confident wrong-doc answers at exactly 0 in every row. Extended with the refusal floor swept over the live endpoint: the reader's best-sentence overlap score is a real confidence signal (correct answers score 0.400–0.875, wrong ones 0.143–0.600, ROC-AUC 0.903), one floor-0 run projects every floor's row and ten live per-floor server runs match the projection exactly, the floor turns out to be a precision knob and never an accuracy knob (accuracy holds its 0.450 ceiling through floor 0.400 and only falls past it, because refusing converts wrong answers into refusals, never into correct ones), and the shipped 0.35 lands in a free window this corpus leaves open — the highest wrong-doc sentence scores 0.333 and the lowest correct answer 0.400, so it blocks all four wrong-doc quotes while eating zero correct answers — but 10 wrong-sentence answers still clear it, and silencing those costs precision-1.000 coverage of just 8/40 at floor 0.65. Extended again with score-gated escalation: a first pass scoring under a trigger retries retrieval at a wider k, billed as two model calls including the suppressed draft, and the trigger at the shipped 0.35 floor catches every query a wider context can fix by construction — fixable means retrieval miss, and every miss scores under the free window's 0.333 ceiling — matching fixed k=10's 0.475 accuracy at 61.5% of its cost, while raising the trigger only adds bill (always-escalate runs 131.8% of fixed k=10 for the same accuracy); but 11 of the 12 escalations buy nothing, because only 1 of the 4 retrieval misses converts even with the whole corpus in context and the other 8 refusals already hold the gold doc, so an oracle escalating just the one convertible query spends 34.3% of fixed k=10 — the measured price of a confidence scalar that says an answer is weak but never why. Extended again with a byte-budgeted event queue: the /ask queue takes its capacity in either currency (event count, or 05's maxBytes budget over serialized wire bytes), the request log gains per-request byte high-water and oversized-admission columns, and the slow-client table re-read over full wire streams (tokens 34-48 B beside ~190 B meta/done JSON payloads) shows a 512-byte budget pinning peak memory at exactly 512 while buffering 14 events to the event cap's 8 at similar memory, a 128-byte budget flooring at the 186 B done event admitted alone — 05's max(budget, largest item) rule holding with a server attached — and a live byte-budgeted server reproducing the event-cap server's eval row field for field, the budget currency changing nothing observable until the client is slow. |
| 21 | [vector-store-persistence](21-vector-store-persistence/) | Vector store persistence and incremental update over 13's HNSW — a checksummed binary format with atomic saves, tombstone and hard deletes, compaction, and a hub-removal attack (Python) | An index is state, not a cache: the on-disk format has to carry the RNG state or a reloaded index silently grows a different graph (levels diverge on 12 of 100 inserts while searches still agree, so the bug hides), incremental insert is bit-identical to a fresh build at 3.09x less cumulative cost because inserting IS how HNSW builds, tombstones hold recall at 0.997 with 70% of the store dead but freeze per-query cost at 304.9 distance computations where a compacted store pays 212.7 (a break-even that collapses from 349558 queries at 10% dead to 18770 at 70%), and hard-unlinking 30% of hubs leaves the heuristic-built graph fully reachable at 0.983 recall while the same attack on a naive M-closest build cuts live reachability to 0.633 and recall to 0.597 — build-time edge diversity is also delete tolerance. Extended with unlink-with-repair, the local patch priced under both attacks: an additive fill patch with heuristic selection holds the naive build's live reachability at 1.000 through 30% removed where bare unlinking falls to 0.633 (naive-selection fill adds nearly the same 34106 edges and still slides to 0.770, so the patch's selection rule matters more than its edge count), a reselect policy that may drop surviving edges ends at 0.135 reachability — repair as an attack with good intentions — the earliest-inserts attack is immune to the one-hop patch (0.639 with or without it, 15012 added edges notwithstanding, because bridges cannot route through the removed batch itself), and heuristic fill costs 1234 distance computations per removed node, 129.8% of the full rebuild that comes back better anyway, so what repair sells at this scale is continuous connectivity per delete, not economy. |
| 20 | [guardrails](20-guardrails/) | Input and output LLM guardrails — regex-plus-validator PII detection and redaction, weighted-rule prompt-injection scoring with de-obfuscation, and a layered pipeline with a canary, all measured against an authored corpus (TypeScript) | Layers help and one leak class lives outside what they can see: a Luhn check and an entropy gate each turn one hard negative from a false positive back into a clean reject (precision 1.000 vs 0.969), de-obfuscation lifts injection ranking from 0.729 to 0.890 ROC-AUC entirely by dragging spacing, leetspeak, homoglyph and base64 attacks from 0% to 100% detection while plain-text rules are unchanged, and stacking an input gate with a canary output check cuts undetected leaks from 2 to 1 — where the stubborn 1 is a paraphrased system prompt that shares no tokens with the canary, so no string-level filter can catch it. |
| 19 | [eval-regression](19-eval-regression/) | Eval harness with regression gating — persisted run records, a paired-bootstrap CI gate, a per-slice gate, and Bonferroni / Benjamini-Hochberg corrected slice gates, with false-alarm and detection rates measured against scripted model versions whose ground truth is known (Python) | An eval score is a sample, not a fact: same-model reruns swing up to 7 points on 240 items, so a 1-point threshold gate false-alarms on 40% of clean reruns where the CI gate holds 2%; a slice regression sized to vanish from the aggregate (one category -24 points, five others +4.8) walks past the aggregate CI gate in 96% of runs and only the per-slice gate catches it (68%), paying a 16% false-alarm rate for six uncorrected intervals; correcting those intervals cuts the false alarms to 4% at the price of masked detection falling to 50% — with BH identical to Bonferroni in every cell because no scenario regresses several slices strongly at once, the one shape BH's step-up is for — and the power curve prices the blindness: a 3-point drift is detected 23.3% of the time at 240 items, needing ~3840 for 93.3%. |
| 18 | [semantic-caching](18-semantic-caching/) | Semantic response caching with hashed lexical embeddings — a cosine threshold sweep pricing dollars saved against wrong answers served, on traffic built around near-miss intent siblings (TypeScript) | A lexical "semantic" cache is a fuzzy-exact cache wearing the wrong name: true paraphrases score below the unrelated-intent control (recall 0.0% at every threshold) while near-miss siblings like "reset my password" / "reset my api key" outrank them in 99.2% of comparisons, so no threshold reaches the queries the cache exists for — yet the fuzzy-exact band still pays real money (81.4% saved at threshold 0.80 vs 47.1% for exact matching, and savings hold at 79.1–82.4% across 20 seeds where wrong serves swing 0 to 9, so cost is measurable off one replay and risk is not), wrong serves are non-monotone in the threshold because the cache's contents are themselves policy-dependent, and char trigrams beat word features on typos (233 vs 157 of 287) while scoring enable-vs-disable 2fa at 0.892. Extended with the serve-margin rule the first sweep left as a question: at margin 0 the gap between the best entry and the closest entry holding a different stored answer really does separate right serves from wrong ones (ROC-AUC 0.894 for word at 0.50, 0.963 for char at 0.75, whose wrong serves are crowded sibling near-misses sitting a median 0.049 behind), but scope decides everything, since the naive over-all-entries margin refuses 177 serves of which 176 were right to fix a single wrong one (savings 86.4% to 78.6%) while the answer-aware scope refuses 4; scoped right, margin 0.10 cuts word 0.50's wrong serves 93 to 13 for 1.5 points of savings and char 0.75's 34 to 0 for 2.1, and across 20 seeds word 0.75 with margin 0.10 dominates bare word 0.80 on both axes (mean wrong 1.00 vs 1.15, saving 84.6% to 86.6% vs 79.1% to 82.4%), so the margin is the better knob than the threshold it replaces; but refusals insert the phrasing they refused, so the margin inherits the store's policy-dependence outright (char 0.50 serves 11 wrong at margin 0.15 and 22 at 0.20), no margin catches the confidently wrong serve with no close competitor, and none of it can be tuned offline from a margin-0 capture, because a refusal changes what the store becomes. |
| 17 | [confidence-calibration](17-confidence-calibration/) | Confidence scores measured against actual accuracy with reliability bins and ECE, repaired with temperature scaling, priced through an escalation policy, then broken by distribution shift (Python) | A confidence score is a promise about frequencies, and an overfit model breaks it: 919 of 1200 test predictions claim 0.990 confidence and deliver 0.886 accuracy, one validation-fitted temperature (T=3.060) cuts ECE 0.130 to 0.030 while moving zero predictions, an answer-iff-confident policy at t=0.90 goes from answering 76.6% of traffic at 0.886 accuracy (promise broken) to 37.9% at 0.980 (promise kept, and the loss surfaces honestly as coverage), and distribution shift explodes ECE to 0.342 where the stale temperature recovers only 0.140 — calibration is a property of the traffic, not the model. Extended with the three cheap trust signals compared as orderings over the same predictions: max softmax, margin (top1 minus top2), and negative entropy disagree on under 2% of item pairs and land within 0.0007 AURC of each other (0.0675 / 0.0680 / 0.0673), so which one ranks mistakes last is a third-decimal decision, while the oracle floor at this accuracy is 0.0227 — two thirds of the achievable mistake-ranking is information no function of one probability row carries; the fitted T=3.060 reorders each signal cross-row (4.05% of max-softmax pairs) and mildly improves every AURC, the logit margin is the temperature-proof ordering for free (pair disagreement under scaling exactly 0), and on shifted traffic the 5% error budget that covered 55.2% of test traffic covers 2.3% — a trust signal quietly hands back an almost-empty answer set instead of detecting shift. |
| 16 | [llm-as-judge](16-llm-as-judge/) | LLM-as-judge eval harness — scripted judges with authored biases (leniency, position, verbosity, self-preference) run through pointwise and pairwise protocols that have to catch them (TypeScript) | Each eval mode is blind to a different bias class: a judge that passes 0.955 of answers still scores 0.745 accuracy against a 0.700 base rate (kappa 0.198 tells the truth, and always-pass gets 0.700 for free), presenting the incumbent first drags a true 0.500 challenger win rate to 0.380 under a position-biased judge — order randomization takes back most of that for free (0.440) and asking in both orders takes back the rest (0.485) at exactly 2x ($2.53 vs $1.26 per 1k pairs) — and self-preference (0.600 measured, 0.500 true) survives order debiasing untouched because it rides on answer identity, not position. The direction extension reads which way both-order flips point: position lean (first-win rate minus 0.5) separates bias from noise where flip rate conflates them, coin flips 0.507 of pairs with lean 0.000 exactly (38 flips each way) while primacy flips 0.287 all one direction for lean +0.143; lean tracks an authored bonus monotonically but reads the same 0.15 bonus as 0.117 or 0.167 depending on the noise floor, a detector with a sign rather than an estimator; and a bonus-vs-exact-gap suppression map puts the verdict-flip knee at gap = bonus, challenger truth 0.500 read as 0.015 at gap 0.05 and 0.500 by gap 0.30 under a 0.15 bonus. The power extension prices the second call: a randomized single call is a coin-flip draw of the pair's two both-order calls, so both leans estimate the same quantity and the difference is pure variance — clean pairs cancel to exactly 0.5 first-wins under both-order but cost a full Bernoulli order draw per single call, worth a 43.2x null-variance ratio at noise 0.04 (both-order detects a 0.09 bonus 82.2% of the time on 100 calls where the equal-budget single-call design reads 16.4%) collapsing to 3.2x at 3x noise since noise makes every pair a swing pair — and an as-stored single call on a 90/10 arrangement reads 0.395 of fake lean at zero authored bonus, which is why randomization is the entry fee for the cheap protocol at all. |
| 15 | [embedding-quantization](15-embedding-quantization/) | Float32 vs int8 vs int4 vector storage, recall measured against memory, with float rerank recovery and two authored failure modes (Python) | Int8 is nearly free and int4 is not: 4x less memory costs about 1.5 points of recall@10 while 8x costs 20, a 20-fetch float rerank makes int8 exact and 50 fetches make int4 exact (8x smaller RAM at recall 1.000), the quantization gap under HNSW holds constant across the whole ef sweep so no search budget buys it back, and the two schemes fail as duals: one rogue near-constant dimension crushes per-vector scaling to 0.515 recall while five rogue vectors stretch the per-dimension grid to 0.649, each leaving the other scheme untouched, and a quantile fit recovers the grid to 0.983 by clipping the outliers it cannot tell from customers. |
| 14 | [context-window](14-context-window/) | Context window management under a token budget — sliding window vs pinned head-and-tail vs extractive summarization of evicted turns, measured on planted-fact retention (TypeScript) | What the summary keeps depends entirely on what "important" means: Luhn's frequency salience makes summarization *worse* than a plain sliding window (71.3% vs 74.6% fact retention, and more summary budget makes it worse) because a decision stated once is the rarest thing in a transcript, while rarity-based salience at the same budget and cost lifts long-lag retention from 23.8% to 57.5% — and every managed policy holds per-call cost flat where full history's call nearly doubles between turn 15 and turn 30. Extended with the incremental running summary, the production shape that folds evicted turns into a carried summary once and can never revisit what it dropped: irreversibility is priced entirely in long-lag retention and only where the summary was earning its keep (rarity at a tight budget on 60-exchange conversations loses 14.6 points, the long-lag column collapsing 40.0% to 3.8%, while Luhn loses 0.0 because its summary held chatter, not facts), the retention edge recompute keeps costs it 3.4x-13.5x the re-read tokens on a bill that grows superlinearly with conversation length, and in one small-share corner lock-in wins: a fact packed early survives re-rankings that recompute later loses it to (76.3% vs 75.8%). |
| 13 | [ann-hnsw](13-ann-hnsw/) | HNSW approximate nearest neighbor from scratch vs an exact scan, recall measured against distance computations (Python) | The recall/cost curve is the whole product: 0.979 recall at 18.5x fewer distance computations, 0.995 at 15.9x, and the last half percent costs more than the first 97.9% did; the paper's neighbor-selection heuristic turns out to be load-bearing, since linking each node to its M closest neighbors instead strands 145 of 2000 nodes on clustered data and drops recall to 0.809, while wall clock at n=3000 is a near-tie with numpy's vectorized full scan because per-node interpreter overhead eats the algorithmic win. |
| 12 | [groundedness-scoring](12-groundedness-scoring/) | Lexical groundedness scoring — flagging RAG answer claims the context doesn't support, measured per hallucination category (Python) | Surface similarity ranks hallucinations above the truth: on minimal-edit hallucinations, tf-idf sentence cosine gives unsupported claims a *higher* mean score than supported ones (0.762 vs 0.668, AUC 0.432) because a swapped entity keeps a sentence's words while an honest paraphrase loses them; a numeric-consistency gate adds a precision-1.000 detector for swapped numbers, negation parity buys 7/7 flip recall at the price of zeroing every legitimate negated paraphrase, and a bag-of-words-identical reordering ("30 days hot / 13 months cold" transposed) scores a perfect 1.0 that no threshold can reach. |
| 11 | [prompt-caching](11-prompt-caching/) | Prompt-cache cost simulation — prefix matching, breakpoint strategies, TTL, and pricing multipliers replayed over seeded agent workloads (TypeScript) | Caching is a bet on repetition and the pricing makes losing it expensive: incremental breakpoints save 78.0% on a multi-conversation agent workload, but one volatile header line flips the same traffic to 1.250x of not caching at all, unique one-shot prompts pay exactly the 1.25x write premium for zero reads, a 5m-TTL cache whose turn gaps pass 5 minutes becomes a pure 1.250x loss, and a tool-heavy turn that outruns the 20-block lookback quietly rewrites the whole history every turn until one extra marker restores the win. Extended with the volatile-position sweep: a per-request state block only poisons the cache from its own index onward, so the same 120-token block costs 0.722x rendered right after the system prompt but 0.301x rendered at the tail (stable floor 0.255x) when breakpoints track it, a 2.4x swing from assembly order alone — while placement-oblivious breakpoints print one flat 0.886x row at every position because no marker sits between the static prefix and the poison, and at position 1 the only stable prefix (882 tokens of tools) dips under the 1024-token minimum so the aware strategy's lone breakpoint silently caches nothing. |
| 10 | [chunking-strategies](10-chunking-strategies/) | Fixed vs sentence vs overlap chunking, measured on BM25 retrieval over an authored ops corpus (Python) | Boundary placement decides retrieval, but it is the metric that collapses, not the ranking: fixed-80 windows split 42.5% of gold answers and 17 of their 20 misses are splits rather than ranking failures, yet the chunk carrying the biggest piece of a split answer still comes back at k=5 for 14 of those 17 (7 at rank 1) and scores zero anyway because containment leaves the relevant set empty. Sentence packing holds splits at zero for the same index size (hit@5 0.850 vs 0.500), and overlap buys back 15 of 17 splits for +30.4% index growth while newly splitting one answer, because overlap moves every boundary instead of only adding windows. |
| 09 | [concurrency](09-concurrency/) | Batching, worker pools, and bounded parallelism over a simulated LLM batch API, on a virtual clock (TypeScript) | Every scaling knob has a sharp knee: client workers past the server's 8-call cap hold throughput at 79.3 req/s while doubling observed latency per doubling of workers (100ms to 793ms p50), batch size 8 captures 90% of the cost amortization batch 32 gets while a call's duration and an item's wait point opposite ways on the same sweep (call p50 100ms to 724ms as the batch grows, item p50 3040ms down to 771ms, because a queued item was going to be waiting anyway — the latency batching really costs shows up only when items arrive over time, +175ms p50 for a 55% cut), and when one unnamed bad item fails a whole 32-item call, bisection isolates it in 11 calls vs 33 one-by-one, an advantage that inverts by 4 bad items because failing halves repay the prompt overhead at every level of the tree. Extended with flaky poison, items that fail probabilistically per attempt: 250 seeded trials per cell show the predicted bisect collapse never happens, because a slice passes whenever its flaky items miss their draws and retires every member while one-by-one pays its fixed ~33-call floor — bisect wins calls across the whole rate-by-count grid (worst ratio 0.78 at 4 items flaking 90%) and completes more flaky items at a third of the calls (61.2% vs 38.8%), the lone token crossover (1.03) sitting in the near-deterministic corner; retry-whole meanwhile flips from pure waste to the cheapest full recovery below a 30% flake rate, then collapses all-or-nothing with healthy items hostage to the flaky ones. Extended again with the timeout-retry storm: an open-loop arrival stream, a client timeout that abandons without cancelling, and 06's backoff policies wired into the queueing model show that a 15s 5x capacity dip under 25/s of arrivals is survivable without retries (66.9% ok, recovered 15s after the dip) but permanent with them — immediate, fixed, exponential, and full-jitter backoff all print the identical row (22.6% ok, NEVER recovers, 94.5% of attempts served to nobody, $33.21 vs $2.73 per 1k completions) because five attempts per task is 125/s of load against a 40/s server and spacing changes when attempts land, not how many there are, so the fix that won 06's herd problem does nothing for a volume problem; a 10% retry budget restores convergence by arithmetic (27.5/s < 40/s) yet still loses to no-retry during pure overload, and earns its keep only against per-attempt flake, where retries lift 80.2% to 100.0% and the budget's ratio has to clear the background failure rate or it starts charging completions (20% flake: 260 denials, 88.4%). Extended a third time with cancellation propagation: an AbortSignal wired through the semaphore and the api lets a firing timeout dequeue an attempt still waiting for a server slot (never admitted, never charged) while in-service work stays unkillable, and the same dip splits the table in two: for capped policies cancellation is a rescue (no-retry 66.9% to 84.4% ok, recovery lag 15.0s to 0.0s, the post-dip queue 259 to 25, because the queue can hold at most one timeout of live attempts), but every unbudgeted retry storm still prints NEVER at amplification 4.10, five attempts per task being volume that cancellation never reduces; what collapses is the bill (wasted service 94.5% to ~27%, drain 250.1s to under 102s, $33.21 to ~$10 per 1k completions), so cancel bounds the queue, budget bounds the volume, and the two fix different halves of the same fire. Extended a fourth time with 06's circuit breaker imported unchanged as a shared fail-fast gate in front of the storm loop: acting on error-rate evidence instead of volume accounting makes it the only mechanism in the study that touches first attempts, so the same dip goes from NEVER (unbudgeted jitter, amplification 4.10, $33.21 per 1k completions) to 85.2% ok with 0.0s recovery lag at $1.89, beating even no-retry's 66.9% because shed arrivals never become the orphan backlog that kept the post-dip queue busy for 15 extra seconds; the half-open probe genuinely reopens the storm mid-dip (a drained server answers one 500ms probe under the 1s timeout, the breaker closes, 25/s lands on 8/s of capacity and re-trips within five timeouts) but the flap is priced and small, with the cooldown knob forgiving anywhere between 2s and 5s; sustained overload turns the flap into duty-cycled load shedding (23.2% to 78.6% ok at 104% load, 4.1% to 37.8% at 125%, at half the attempts); and the trip threshold is a knife edge on healthy-but-flaky traffic, where threshold 3 sheds 1180 healthy tasks for 47.6% ok on a 20% flake stream that retries alone absorb at 100.0%, one threshold step from a breaker that never fires and costs nothing. |
| 08 | [agent-tool-loop](08-agent-tool-loop/) | Agent loop with zod-validated tool calling and hard retry/failure policies, each malformed-arg flaw priced against a clean run (TypeScript) | Validation strictness is a budget knob: failing on the first invalid call completes 10/25 tasks, feeding zod errors back recovers 22/25 at 3x the token spend, and a loop guard that aborts on the 3rd identical invalid call keeps the recovery while cutting 80.9% of the tokens a never-correcting model burns, at the price of killing the one slow corrector plain feedback would have saved. Extended with a drift study: against a model that mutates its broken call every round, the exact-identity guard saves exactly 0.0% (it never fires once), a guard keyed on the zod issue signature (paths and codes, not values) gets 61.6% back but kills both slow correctors that fail the same way for three rounds before correcting — and the sweep shows the trip limit is the real knob, since limit 4 spares every corrector while still cutting stubborn burn 44.2%, with the signature key proven a strict widening (byte-identical outcomes on all 25 original tasks, same seeds). Extended again with a caching study: the same runs repriced under a warm prompt cache (reads 0.1x, writes 1.25x, breakpoint after every request) show the guard's 80.9% token saving was already only 73.6% in dollars, drops to 61.9% cached, and floors at 59.7% even with free reads, because the stubborn burn's real cost is output tokens and cache writes, not the replayed history the token count pointed at; anyone justifying a loop guard by multiplying history length by input price is overstating its value roughly 2x. Extended a third time with the dual failure mode: every arg validates and one tool result per task comes back corrupted (lie ×10, empty string, HTML garbage, 20000-char bomb, transient) under three policies — result validation off, reject on first invalid result, rerun twice then fail. Mid-chain garbage is caught anyway but one hop late at 22x the tokens (the propagated NaN fails the next call's arg schema and the loop guard fires at 374 tokens-in vs 17 at the boundary), the empty string slips even that net because Number("") is 0, the bomb bills 15341 tokens-in against 17 rejected unseen (902.4x — every later model call re-reads it from history), the last call of a chain has no downstream net at all, and the two shapes that actually ship wrong answers — a plausible lie at exactly clean-run cost and corrupted free text — pass every schema under every policy: shape-checking results is cheap insurance against loud garbage and token bombs, not a groundedness check. |
| 07 | [near-duplicates](07-near-duplicates/) | MinHash + LSH banding vs SimHash for near-duplicate detection, measured against exact Jaccard ground truth (Python) | The sketch should narrow the search, not issue the verdict: well-placed banding (b=64 r=2) recovers all 360 labeled duplicate pairs at 3.5% of brute-force comparisons, one step of mistuning (s-curve at 0.383, above the 0.280 duplicate floor) silently drops 31 pairs no verification can recover, and SimHash's 64-bit fingerprint tops out at F1 0.942 because its duplicate/non-duplicate Hamming tails overlap structurally. |
| 06 | [rate-limiting](06-rate-limiting/) | Exponential backoff and jitter under a simulated thundering herd, on a deterministic virtual clock (TypeScript) | Jitter fixes synchronization, not volume: no-jitter retries collide 20-wide in lockstep while full jitter never exceeds 1, yet full jitter's peak load is *higher* because its mean delay is half; honoring Retry-After finishes closest to the ideal makespan (9.47s vs 9.0s), and client-side pacing cuts attempts-per-success from 2.99 to 1.11 by moving the waiting into your own process. Extended with outage studies: a server-jittered Retry-After hint kills the exact hint's 12-wide re-synchronization at no makespan cost that survives a 5-seed sweep (which also exposes that 9.47s as the best of its 5 seeds), a hard 10s outage flips jitter from cure to liability under a fixed retry budget (no-jitter survives 100% where full jitter's short draws leave 67.5%, and nobody guessing outlives the 22.7s budget cliff), and a dead service burns exactly the retry budget under every backoff policy — 1080 attempts as a 2.40s hammer or a 68.10s drip, the argument for a circuit breaker. Extended again with that breaker built and priced: fail-fast tripping collapses the dead-service bill from 1080 to 200 wire attempts at k=5 (later requests hear "no" in 0.00s instead of 11.33s), but the identical trip turns a survivable 5s outage from 100% success into 0%, wait-mode probes fire at max(backoff, cooldown) so the cooldown acts as a delay floor that stretches the same 9-attempt budget to survive a 20s outage (100% at a 5s cooldown vs 5% plain), and a breaker that counts 429s as failures false-trips the healthy herd down to 15.5% success while counting only 503s reproduces the baseline to the attempt, so the failure predicate and the cooldown, not the trip threshold, are the knobs that matter. Extended a third time with the pacing knee and AIMD: sweeping fixed pacing from 80% to 120% of the budget finds a knee at 100% that is sharp (throughput flatlines while 429s jump 3 → 1142) and fragile (standing exactly on it turns a 2% transient fault rate into 96 429s via the unpaced-retry bypass, so 95% is the real operating point), and when the budget drops from 20 to 8 req/s mid-run, an AIMD pacer starting blind converges within one 5s sample, pays a 10.9% makespan tax over the schedule-following oracle for a 429 bill an order of magnitude under unpaced — and beats that oracle on success rate (99.9% vs 98.9%), because the informed client stands exactly on the fragile knee while the AIMD sawtooth's troughs are accidental safety margin. Extended a fourth time with RateLimit headers on the wire: trusting the limit header cuts AIMD's 11.0% makespan tax to 2.5% and its 199 429s to zero (one response makes you the oracle), a remaining-only estimator that counts refill from token deltas plus its own admissions recovers the entire 429 bill at 95% headroom but none of the discovery ramp, and its signal is asymmetric by construction: an empty bucket teaches the exact refill rate, a full one censors it down to a single probe-worthy bit, so capacity drops are learned by arithmetic and capacity raises only by probing. The headroom sweep's control row also re-attributes the oracle's famous 1.1% failure rate: it was never the 100% rate, it was the 20-wide t=0 burst, and shaping the burst (1.88s) is cheaper than 5% of margin (6.43s). Extended a fifth time with the production-standard trip detector: the breaker's gate machinery now takes a rolling-window error-rate detector (50% over 1s behind a 20-settle volume floor) beside the consecutive counter, and a 20-seed sweep over a healthy-but-flaky server separates them exactly where the open thread predicted — at 30% transient faults the k=5 counter false-trips in 17 of 20 runs on chance streaks (52.7% success, every fast-failed request one that retries would have saved) while the rate detector trips in 3 (90.8% success), and both fire 20/20 on a real 70% brownout. The window's own price is volume: per-client it can never fire (the full 1080-attempt no-breaker bill), counting 429s it trips on the herd burst's genuine 50% rejection rate exactly like the counter, and behind a shared wait-mode gate one probe per cooldown lets the whole herd outlive even the 30s outage (100% vs 0% plain) at the price of unbounded hang time. |
| 05 | [token-streaming](05-token-streaming/) | Streaming LLM output in TypeScript — SSE byte parsing, partial JSON for tool arguments, backpressure, all fuzzed at chunk boundaries | The network hands you bytes, not tokens: 300/300 random chunkings parse identically once CR-on-a-boundary and split UTF-8 are handled, partial parsing puts the first tool-call field on screen at 44% of the stream instead of 100% (though an object-valued field parses as `{}` before it holds anything, so "first parsed" and "carries a value" are two different columns), and a bounded queue turns O(stream) buffering into O(1) at zero wall-time cost when the consumer is the bottleneck. Extended with a resumable partial-JSON scanner that carries its state between fragments instead of rescanning: same answer as the baseline at every fragment boundary (300/300 chunkings), 3.4x faster at 266 chars rising to 662.5x at 64KB, so there is no crossover size to wait for, and the quadratic bill quietly returns if you deep-copy a snapshot per fragment. Extended again with a byte budget on the queue: chunk counting is an honest memory bound only while sizes are near-uniform; on a heavy-tailed workload where 0.8% of chunks carry 63.0% of the bytes, an 8-chunk cap's peak memory is ordering luck (35570 to 221864 bytes by arrival order against a 261648-byte worst case) while a 65536-byte cap pins exactly 65536 under every ordering, at the same 64 KB guarantee the 2-chunk cap starves the consumer 858 idle ticks where the byte cap starves it 0, and the one bound a byte budget cannot hold is the largest single chunk, admitted alone instead of deadlocking, so a 4096 budget peaks at the 32706-byte chunk. Extended again with caps on the two buffers a hostile stream can grow without bound, the incomplete line and the in-flight event's accumulated data: an unterminated 4 MiB line that an uncapped parser retains in full (4194310 chars, 0 events, forever) becomes either a typed SseLimitError at 1.6% of the poison (fail closed, parser poisoned) or one dropped line with all 40 surrounding events delivered at a 65650-char high-water against the cap+chunk bound (skip mode); the line cap alone misses accumulation across terminated lines, 2100 short lines still pile up 1077299 chars, which is what the separate event cap is for (high-water 65803), and 300/300 fuzz chunkings pin the caps as free on well-formed streams. |
| 04 | [bpe-tokenizer](04-bpe-tokenizer/) | Byte-level BPE from scratch — vocab size, training domain, and script, each measured against token cost | The tokenizer is a silent price multiplier: a prose-only tokenizer pays 80.7% more for the same code, CJK costs 9x English per character when training never saw it, and byte fallback holds OOV at a structural 0% while a word baseline loses 20.3% of held-out tokens to `<unk>`. |
| 03 | [hybrid-search](03-hybrid-search/) | BM25 vs dense retrieval (LSA) vs rank fusion, measured with recall@k and MRR | Dense wins paraphrase queries (MRR@10 0.793 vs 0.765), both ace exact identifiers — corpus-fit LSA can't be out-of-vocabulary — and RRF fusion takes the best overall MRR@10 (0.899) at the price of rank-blind averaging. |
| 02 | [retrieval-eval](02-retrieval-eval/) | BM25 vs TF-IDF cosine from scratch, measured with recall@k and MRR, plus paired bootstrap CIs on the gap | BM25 never loses a query to TF-IDF here — but the bootstrap shows the MRR gap (0.934 vs 0.917) rests on 2 of 38 queries and its 95% interval touches zero, while the b=0 length-norm ablation gap survives resampling. Measured beats claimed. Extended with an inverted index pinned bit-identical to the flat scan (same floats, 38/38 golden queries) and a zipf-corpus scaling study: the full scan degrades linearly to 65.229ms/query by 32k docs, yet the inverted index is only a flat ~2.8x touch-ratio win on typical queries, because the single most common term in a query owns 64.9% of the postings it touches; rare-term queries get 382.6x, since query cost is the sum of the terms' document frequencies rather than corpus size, which is the measured case for stopword handling and WAND-style pruning on top of posting lists. That pruning is now measured too: WAND and MaxScore with exact per-term score bounds return bit-identical top-10 results while scoring only 30-32% of the common-heavy postings bill and 10.5% of the typical one, decaying to 59% scored by k=1000, and in pure Python the wall clock follows the counts only on typical queries (2.1x), the interpreter-overhead lesson from 13 again. Extended once more with block-max WAND: an exact max gain stored per posting block lets the pivot skip inside a list, cutting the common-heavy floor from 29.5% to 9.0% of the bill at block size 8 (13.1% directory overhead) and typical traffic from 10.5% to 6.4%, still bit-identical on every query — with the twist that the head term gains nothing from blocks because idf already flattened its bound, the tightening lives in the mid-common ranks, and the shallow checks that buy the skips outnumber the postings they save, so the deepest cut is also the slowest searcher in Python while being exactly what native engines ship. |
| 01 | [structured-output](01-structured-output/) | Schema-validated LLM output with malformed-output retry | Free string-level repairs took success from 20% to 60%; retries took it to 96.7% at a 47% call overhead — and the two layers should never be confused. The blind-retry control ties on all three columns, identical ticket for ticket, because the scripted model never reads the prompt: that number prices retrying, not feedback, and separating the two needs a real model. |

Each project folder is self-contained: `cd` into it and its README tells
you how to run everything. Projects don't share installed dependencies;
the one exception to full isolation is that a project may import a tiny
utility from an earlier one (06 imports 05's seeded PRNG; 08 and 09
import 06's virtual clock; 11 imports 08's token estimator; 12 imports
02's TF-IDF and 10's sentence splitter; 13 imports 02's recall@k; 14
imports 08's token estimator and pricing; 19 imports 02's paired
bootstrap; 25 imports 02's BM25 and 23's PRF term extractor) rather
than keep a second copy
of the same algorithm in the same language.
